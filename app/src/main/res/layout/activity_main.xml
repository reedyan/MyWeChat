<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


        <ImageView
            android:id="@+id/wechat_background"
            android:layout_width="0dp"
            android:layout_height="302dp"
            android:contentDescription="@string/background"
            android:scaleType="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <TextView
            android:id="@+id/user_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="14dp"
            android:layout_marginBottom="19dp"
            android:textColor="@color/white"
            app:layout_constraintBottom_toBottomOf="@+id/wechat_background"
            app:layout_constraintEnd_toStartOf="@+id/avatar_page" />

        <ImageView
            android:id="@+id/avatar_page"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="210dp"
            android:layout_marginEnd="20dp"
            android:contentDescription="@string/todo"
            android:scaleType="center"
            app:layout_constraintEnd_toEndOf="@+id/wechat_background"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/all_recycleView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:scrollbars="vertical"
            app:layoutManager="LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/avatar_page" />

</androidx.constraintlayout.widget.ConstraintLayout>